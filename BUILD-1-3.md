# Superseded

This file covered steps 1 to 3 only. The complete twelve-step spec is in
`BUILD.md`, which contains everything that was here plus steps 4 to 12, the
invariants section, the dependency graph and the build gates.

Delete this file.
